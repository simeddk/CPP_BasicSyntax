#pragma once

#include <iostream>
#include <Windows.h>
#include <string>
using namespace std;

enum class EStateType { Idle, Approach, Attack };
enum class EDirectionType { None, Left, Up, Right, Down };
